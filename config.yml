sql:
    default: 'postgre'
    postgre:
        lib: 'psycopg2'
        args: []
        kwargs:
            host: "localhost"
            port: 5431
            database: "mimic"
            user: "mimicuser"
            password: "mimicuser"
    fhirbase:
        lib: 'psycopg2'
        args: []
        kwargs:
            host: "localhost"
            port: 5433
            database: "fhirbase"
            user: "postgres"
            password: "postgres"
    oracle:
        lib: 'cx_Oracle'
        args: ['user/password@database']
        kwargs: {}

graphql:
    server: 'https://graphql.pyrog.staging.arkhn.org/'
    token: "<token>"
